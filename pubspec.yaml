name: gestor_aplicacion
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.12.0 <4.0.0'         

dependencies:
  flutter:
    sdk: flutter
  bcrypt: ^1.1.3
  dart_jsonwebtoken: ^2.14.0
  flutter_test:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
